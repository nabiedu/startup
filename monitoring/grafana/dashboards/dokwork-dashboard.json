{
  "annotations": {"list":[]},
  "panels": [
    {
      "type":"graph",
      "title":"HTTP requests total",
      "targets":[{"expr":"http_requests_total","format":"time_series"}],
      "id":1
    },
    {
      "type":"graph",
      "title":"HTTP request duration (seconds)",
      "targets":[{"expr":"histogram_quantile(0.95, sum(rate(http_request_duration_seconds_bucket[5m])) by (le))","format":"time_series"}],
      "id":2
    },
    {
      "type":"graph",
      "title":"Active users (approx)",
      "targets":[{"expr":"sum(http_requests_total) by (instance)","format":"time_series"}],
      "id":3
    }
  ],
  "title":"DOKWORK Overview",
  "schemaVersion":16,
  "version":1
}
