# ⚡ QUICKSTART — Запуск за 3 минуты

## Windows (PowerShell)

```powershell
# 1. Откройте PowerShell в корне проекта и запустите скрипт:
.\start-local-dev.ps1

# ✅ Сервис запустится автоматически и откроет frontend в браузере
```

## Linux / macOS

```bash
# 1. В терминале (корень проекта):
bash start-local-dev.sh

# ✅ Сервис запустится, откроет frontend в браузере
```

---

## 🎯 Что произойдёт автоматически

1. ✅ npm install (если нужно)
2. ✅ Генерация Prisma Client для SQLite
3. ✅ Применение миграций БД
4. ✅ Загрузка примеров данных (seed)
5. ✅ Запуск backend на :4000
6. ✅ Открытие frontend в браузере (file:///...)

---

## 🔑 Учётные данные для входа

После запуска скрипта в браузере:

| Email | Пароль | Роль |
|-------|--------|------|
| admin@dokwork.kz | admin123 | admin |
| user@dokwork.kz | user123 | user |

---

## 🧪 Проверка API

После запуска backend'a, в отдельном терминале:

```powershell
cd backend
npm run test:endpoints
```

Ожидаемый результат: все тесты должны пройти (зелёные ✅).

---

## 🛑 Остановка проекта

- **Windows**: `Ctrl+C` в окне PowerShell
- **Linux/macOS**: `Ctrl+C` в терминале

---

## ❓ Если что-то не работает

| Ошибка | Решение |
|--------|---------|
| `Cannot find module 'dotenv'` | `cd backend && npm install` |
| `Port 4000 already in use` | Закройте другой процесс на порту 4000 или используйте `PORT=5000 npm run start:sqlite` |
| Frontend не видит Backend | Убедитесь, что backend запущен, откройте http://localhost:4000/health в браузере |
| SQLite migration fails | Удалите `backend/dev.db` и попробуйте снова |

---

## 📚 Полная документация

- 📖 **README.md** — полное описание проекта
- 🚀 **DEPLOYMENT.md** — как развернуть на production

---

**Готово! 🎉 Проект работает локально. Начните разработку!**
